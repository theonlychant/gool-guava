# Barmix combined tests

This folder contains additional test suites for the `cpuhelper` libraries.

Go tests:

```bash
cd barmix/golang
go test ./... -v
```

Java tests:

```bash
cd barmix/java
gradle test --no-daemon --console=plain
```
